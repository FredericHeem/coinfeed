define(['jquery', 'underscore', 'backbone'], function($, _, Backbone) {

    var FeedModel = Backbone.Model.extend({
        defaults: {
        },

        initialize: function() {
        }
    });

    return FeedModel
});
